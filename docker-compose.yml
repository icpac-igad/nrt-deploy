version: "3.8"

services:
  nrt_deploy:
      build:
        context: .
      container_name: nrt_deploy
      restart: ${RESTART_POLICY}
      env_file:
        - .env
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
        - ./data:/opt/nrt-script/data